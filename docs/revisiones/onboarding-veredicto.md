# VEREDICTO revisor-visual — onboarding
Fecha: 2026-08-15 00:00
Screenshot: docs/revisiones/onboarding-375.png
Usabilidad: 17/40
Craft: 3/20
Copy (si vende): N-A
Fidelidad (si hubo referencia): N-A
Veredicto: NO LISTA
Top defectos:
1. [Pantalla completa — renderizado, BLOQUEANTE] El screenshot actual muestra la pantalla prácticamente en blanco: de todo el contenido de `PantallaNombre` (app/onboarding/page.tsx líneas 338-422) solo se ven el header (flecha atrás, barra de progreso, "Omitir") y una línea de pie ("Pregunta 1 de 6 · Tus respuestas ajustan el plan de tu hijo"). La mascota (`MascotaMini`, línea 381), el título "¿Cómo se llama tu hijo o hija?" (línea 384-386), el subtítulo (línea 388-390), el campo de nombre con su label (línea 391-401), el botón "Continuar" (línea 402-410) y la línea de confianza con ShieldCheck (línea 411-414) — todos presentes en el código — NO aparecen en la imagen. Causa más probable revisando el código: TODOS esos elementos están envueltos en `motion.div/label/button` con `initial={{opacity:0,y:10}}` y delays escalonados hasta 0.3s+0.28s (función `entrada()`, línea 349-356); el único texto visible en el screenshot es precisamente el ÚNICO elemento sin `motion` (el `<p>` final, línea 415-419, sin `initial`/`animate`). Esto apunta a que el screenshot se capturó antes de que terminara la animación de entrada, no a que el contenido esté realmente ausente en producción. Fix: re-capturar esperando a que las animaciones de entrada asienten (ej. `page.waitForTimeout(800)` o esperar opacity computado = 1 en Playwright) antes de tomar la captura, y volver a pedir revisión — sin una captura válida no se puede verificar jerarquía, identidad, encaje óptico, ni siquiera que el input y el CTA existan de verdad en pantalla.
2. [Craft — los 5 ejes, consecuencia del defecto 1] Con el contenido principal invisible, ningún eje observable (jerarquía, profundidad, identidad, encaje) puede aprobarse desde el screenshot entregado — se puntúan cerca del piso no porque el diseño sea malo (el código sugiere un sistema visual cuidado: mascota con contorno "sticker", chips con check custom, CTA con sheen, jerarquía tipográfica clara) sino porque la evidencia visual entregada esta ronda no lo demuestra. Movimiento (EJE 4) se puntúa parcialmente con base en código (stagger, whileTap, spring, prefers-reduced-motion respetado) pero no puede confirmarse que resuelva a un estado visible. Fix: idéntico al defecto 1.
3. [Usabilidad h8/h10 — estético/minimalista y ayuda contextual] Tal como se ve la captura, la pantalla es ~85-90% espacio vacío con el usuario sin ninguna pista de qué hacer (ni título, ni campo, ni botón visibles, solo una barra de progreso y un texto de pie). Si esto reflejara la experiencia real entregada al usuario, sería el peor caso posible de "pantalla muda". Fix: idéntico al defecto 1; si tras re-capturar el problema persiste con animaciones ya asentadas, es un bug real de renderizado (revisar que Motion esté hidratado antes del screenshot, o que `useReducedMotion` no esté devolviendo un valor inconsistente en SSR/CSR).
4. [Código — verificado, informativo] El fix de la regresión crítica de la ronda anterior (el mount-effect saltaba a "resultado" con datos incompletos) se ve correcto en el código: ahora valida las 5 claves requeridas — edad, nivel, meta, momento, minutos (líneas 84-92) — antes de decidir el salto, y si falta alguna reinicia en el primer paso sin responder en vez de saltar al resultado. También se agregó `role="progressbar"` + `aria-valuenow/min/max` + `aria-live="polite"` en el header (líneas 168-174), cumpliendo lo pedido en la ronda anterior. Ninguno de los dos fixes pudo confirmarse VISUALMENTE en esta captura porque el contenido no es observable, pero ambos pasan revisión de código.
5. [Craft — vacío de fondo, heredado, no verificable esta ronda] El defecto de la ronda anterior (~40% del viewport como fondo liso arriba/abajo del bloque de contenido, declarado "no tocado esta ronda") tampoco puede confirmarse ni descartarse en esta captura: el fondo liso ES casi todo lo que se ve, pero por una razón distinta (contenido invisible por timing de animación, no por espaciado de diseño). Pendiente re-evaluar en la próxima captura válida.

Nota para la siguiente ronda: este veredicto NO reemplaza la evaluación de fondo de la pantalla — la pantalla necesita una captura donde el contenido esté efectivamente pintado (opacity:1) para poder juzgar con criterio los 10 puntos de usabilidad y los 5 ejes de craft que dependen de ver el diseño real. Hasta entonces, el gate doble (≥36/40 y ≥16/20) no se cumple con la evidencia disponible.
